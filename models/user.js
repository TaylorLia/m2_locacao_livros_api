// Definindo a classe User
class User {
    // Implementando o construtor da classe
    constructor(
      id,
      name,
      email,
      password
    ) {
      this.id = id
      this.name = name
      this.email = email
      this.password = password
    }
  }
  
  module.exports = User
  